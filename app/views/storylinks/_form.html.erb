<%= form_for @storylink, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :story, :class => 'control-label' %>
    <div class="controls">
      <%#= f.text_field :story, :class => 'text_field' %>
      <%= f.collection_select(:story, Story.find(:all), :name, :name, :include_blank => true) %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :pattern, :class => 'control-label' %>
    <div class="controls">
      <%#= f.text_field :pattern, :class => 'text_field' %>
      <%= f.collection_select(:pattern, Pattern.find(:all), :name, :name, :include_blank => true) %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                storylinks_path, :class => 'btn' %>
  </div>
<% end %>
